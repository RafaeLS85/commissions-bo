import { Category, CategoryApi } from "@/types/categories";

export function mapToCategory(data: CategoryApi[]): Category[] {

    // const categoryMap: {[id: string]: Category } = {};

    // data.forEach(({id, name}) => {

    //   const categoryId = parseInt(id);
    //   const parentId = categoryId % 10 === 0 ? null : Math.floor(category)

    //   categoryMap[id] = {
    //     id: categoryId
    //   }


    // }) 


  return [];
}
